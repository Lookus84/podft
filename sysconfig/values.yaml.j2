#!jinja|yaml|gpg
system_config:
{%- for key, value in merged_config.items() %}
    {{ key }}: "{{ value }}"
{%- endfor %}

system_gpg:
{%- for key, value in secrets.items() %}
  {{ key }}: "{{ value }}"
{%- endfor %}
